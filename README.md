# FootballTrainer
ML pipeline that predicts football match outcomes (Win/Draw/Loss) using feature engineering, XGBoost, and Optuna tuning. Includes a FastAPI prediction endpoint, MLflow experiment tracking, and Docker deployment.
